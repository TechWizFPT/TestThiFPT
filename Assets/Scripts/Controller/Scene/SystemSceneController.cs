using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SystemSceneController : MonoBehaviour
{
    private void Awake()
    {
        //Singleton[] listSingeonObj = FindObjectsByType<Singleton>();  
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
